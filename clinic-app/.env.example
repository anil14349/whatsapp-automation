# Server-side only: never exposed to the browser.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Prefills the clinic field on the sign-in form.
NEXT_PUBLIC_DEFAULT_CLINIC_ID=
