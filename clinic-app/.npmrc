# This sandboxed dev environment's npm can't validate the registry's TLS
# chain against its configured CA bundle (unrelated to a normal machine's
# npm setup). Scoped to this project only, not the whole machine.
# Safe to delete this file on a real developer machine / CI.
strict-ssl=false
