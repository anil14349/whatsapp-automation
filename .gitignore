# Build outputs
.next/
next-env.d.ts
tsconfig.tsbuildinfo
dist/
build/

# Dependencies
node_modules/
.pnpm-store/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment variables
.env
.env.local
.env.*.local
# Backups taken when moving between projects: these hold service role keys.
.env.local.*

# Misc
.cache/
*.pid
# Anchored patterns only matched the root, so a `supabase link` run from
# clinic-app committed a .temp pointing at the retired Tokyo project. Match
# it at any depth.
**/supabase/.temp/
test-webhook-result.txt
webhook-test.txt

# Scratch output from verification runs
.verify-result.txt
.verify-schema-result.txt
.clinic-tsc-result.txt

# Deno lockfile is generated per-machine by the test runner
deno.lock
